/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "i.abcnewsfe.com",
      "images.app.goo.gl",
      "encrypted-tbn0.gstatic.com",
    ],
  },
};

export default nextConfig;
